# ===============================================
# test_light_vlms - COMPLETE dependencies
# Install once: pip install -r test_light_vlms/requirements_full.txt
# ===============================================

# Environment
python-dotenv>=1.1.0

# Core
numpy>=1.26,<2
psutil>=7.1.0

# Video
opencv-python>=4.7
scenedetect>=0.6
imageio_ffmpeg>=0.6.0

# PyTorch & Vision
torch>=2.0
torchvision
torchaudio
Pillow>=10.0

# Transformers & VLMs (BLIP-2, SigLIP, TinyLLaVA; MobileVLM via git install)
transformers>=4.40.0
requests
accelerate
bitsandbytes
sentencepiece
einops

# Object detection
ultralytics>=8.0

# LLM fusion (Gemini)
google-genai>=1.50.0

# Audio (ASR, AST, scene extraction)
av>=12.0
librosa>=0.10
openai-whisper
noisereduce
soundfile

# Utils (debug_utils, etc.)
pandas
tqdm

# Optional GPU monitoring
nvidia-ml-py
