You are writing a SceneWalk-style visual description for one video segment from sparse or noisy evidence.

Write a compact paragraph that follows the clearest visible sequence:
1. setting and main visible subject
2. main action or interaction
3. important shot change or next action, if visible
4. key objects that define the action

Use 1-3 sentences. Include enough visible detail to match a human reference, but avoid unsupported guesses.

Audio is secondary. Use it only when it clearly supports a visible speaker, action, or event. Do not quote long dialogue or infer off-screen plans from transcript text. Omit audio when it does not change the visible description.

Be confident but conservative. Avoid identities, motives, relationships, precise locations, emotions, and facial details unless clearly grounded. Broad clothing or object details are acceptable when stable and useful to the action.

Prefer broad visual descriptors over guesses: "a person," "a man," "a woman," "an indoor hallway," "a road," "near water," "on a pier," or "a room."

Constraints:
- Use one coherent paragraph, not a list.
- Focus on the most reliable visible details first.
- Include the main visible action sequence, not only a generic caption.
- Keep dialogue/audio to at most one short clause, and omit it entirely if the visible action is already clear.
- Remove object detection IDs, coordinates, counts, and bounding-box language.
- Do not use filler such as "the scene feels", "the moment suggests", or "the atmosphere is".
- Do not use uncertainty boilerplate such as "appears to", "possibly", or "it is inferred".
- Keep content PG-13.

VIDEO NAME: {{VIDEO_NAME}}

SCENE INFORMATION:
{{SCENE_TEXT}}

Treat YOLOv8 object detections as potentially noisy; discard or correct them when contradicted by context or captions.
Content must be non-sexual, non-violent, PG-13 or lower, child-safe, G-rated.

Segment description:
