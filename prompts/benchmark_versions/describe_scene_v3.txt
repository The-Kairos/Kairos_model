You are writing a SceneWalk-style visual description for one video segment.

Write a compact paragraph that follows the visible sequence of the segment. Match this structure when possible:
1. Start with the setting and main visible subject.
2. Describe the main action or interaction.
3. If the shot changes, briefly describe the next visible action.
4. Mention key visible objects that define the action.

Use 2-4 sentences for normal segments. Use 1-2 sentences only for very short, static, or text-only segments.

Prioritize recall of visible events over extreme brevity, but stay grounded. Include stable details that help match the reference, such as broad clothing type, carried objects, vehicles, roads, rooms, water, signs, screens, doors, or furniture. Avoid tiny attributes that do not affect the action.

Use audio only when it clearly belongs to a visible speaker or directly clarifies the visible event. Briefly summarize speech only if it changes what the viewer understands about the on-screen action. Do not quote long dialogue or infer off-screen plans.

Be confident but conservative. Do not invent identities, names, motives, relationships, professions, precise locations, emotions, or facial details unless clearly visible and central.

Write in plain observable language. Prefer broad grounded descriptors such as "a person," "a man," "a woman," "an indoor hallway," "a street," "a room," "near water," or "on a pier" over narrow guesses.

Constraints:
- Use one coherent paragraph, not a list.
- Include the main visible sequence, not just a generic caption.
- Avoid ending with empty filler such as "no other notable objects are visible" unless the segment is truly static or empty.
- Do not include detector IDs, coordinates, counts, or bounding-box language.
- Do not use filler such as "the scene feels", "the moment suggests", or "the atmosphere is".
- Do not mention uncertainty boilerplate such as "appears to", "possibly", or "it is inferred".
- Keep content PG-13.

VIDEO NAME: {{VIDEO_NAME}}

SCENE INFORMATION:
{{SCENE_TEXT}}

Treat YOLOv8 object detections as potentially noisy; discard or correct them when contradicted by context or captions.
Content must be non-sexual, non-violent, PG-13 or lower, child-safe, G-rated.

Segment description:
