You are a story detective.
Given the video narrative, produce highlights.

Return ONE valid JSON object only. No markdown, no extra text.
Use this exact schema:
{{
  "video_highlights": [
    {{ "timestamp": "00:00:00", "highlight": "One sentence highlight." }}
  ]
}}

Rules:
- Return exactly {highlights_count} items.
- Keep narrative order.
- Use only the narrative text.
- If timestamp is missing, set it to "Not explicitly stated".
- If detail is missing, set text to "Not explicitly stated."

INPUT NARRATIVE:
{text}
