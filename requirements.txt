# ===============================================
# Note to avoid future compatibility issues: 
# (1) Use a virtual environment.
# (2) Please add version numbers when adding new packages.
# ===============================================

# Environment / .env
python-dotenv>=1.1.0

# General Utils
numpy>=1.26,<2
# pynvml==13.0.1 # logging GPU usage - (FutureWarning: The pynvml package is deprecated. Please install nvidia-ml-py instead)
nvidia-ml-py==12.560.30  # logging GPU usage (NVML official bindings)
psutil==7.1.3  # logging CPU and RAM usage

# General Video Processing
opencv-python>=4.7 #OpenCV for video processing
scenedetect>=0.6 # PySceneDetect
imageio_ffmpeg==0.6.0 # Saving cut clips

# Light Captioning - BLIP 
torch == 2.9.1 
transformers==4.57.1
Pillow== 12.0.0

# Heavy Captioning - InternLM-XComposer2
sentencepiece==0.2.1
torchvision==0.24.1
einops==0.8.1

# Heavy Captioning - Gemini Pro 2.5
google-genai==1.50.1
dotenv==0.9.9

# Object Detection - YOLOv8
ultralytics==8.3.230

# System monitoring / profiling
psutil>=5.9.0

# Audio cutting per scene using ffmpeg
ffmpeg_downloader>=0.3.0
ffmpeg-python>=0.1.20
# Note: ffmpeg must be installed on your system
# For Windows, run: winget install ffmpeg
# Then in your System Environment Variables, add the path to ffmpeg/bin to your PATH variable by:
# 1. Open CMD
# 2. Run: winget install ffmpeg
# 3. Run: where ffmpeg
# 4. Copy the directory path (excluding ffmpeg.exe)
# 5. Open System Environment Variables
# 6. Edit the PATH variable and add the copied directory path
# 7. Restart your VS Code or IDE to apply changes

# Azure OpenAI SDK
openai>=1.9.2

# Google Cloud Audio Intelligence API
google-cloud-videointelligence>=2.10.0
google-api-core>=2.11.0
grpcio>=1.56.0
protobuf>=4.24.0

# Progress bars / helpers
tqdm>=4.66.1